// Generated by CoffeeScript 2.3.2
exports.reverse = function(l) {
  var i, j, ref, reversed;
  reversed = [];
  for (i = j = ref = l.length - 1; (ref <= 0 ? j <= 0 : j >= 0); i = ref <= 0 ? ++j : --j) {
    reversed.push(l[i]);
  }
  return reversed;
};

exports.toPromise = function(fn) {
  return function(...args) {
    return new Promise(function(resolve, reject) {
      var err;
      try {
        return fn(...args, function(err, result) {
          if (err) {
            return reject(err);
          } else {
            return resolve(result);
          }
        });
      } catch (error) {
        err = error;
        return reject(err);
      }
    });
  };
};

exports.toPromises = function(fns) {
  var as_promises, fn, key;
  as_promises = {};
  for (key in fns) {
    fn = fns[key];
    as_promises[key] = exports.toPromise(fn);
  }
  return as_promises;
};
