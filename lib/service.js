// Generated by CoffeeScript 1.7.1
(function() {
  var Binding, ConsulAgent, EventEmitter, Service, VERBOSE, descend, helpers, log, os, util, _,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    __slice = [].slice;

  os = require('os');

  util = require('util');

  helpers = require('./helpers');

  _ = require('underscore');

  EventEmitter = require('events').EventEmitter;

  ConsulAgent = require('./consul-agent');

  Binding = require('./binding');

  log = helpers.log;

  VERBOSE = true;

  descend = function(o, c) {
    if (c.length === 1) {
      return o[c[0]].bind(o);
    } else {
      return descend(o[c.shift()], c);
    }
  };

  Service = (function(_super) {
    __extends(Service, _super);

    function Service(name, methods, options) {
      this.name = name;
      this.methods = methods != null ? methods : {};
      if (options == null) {
        options = {};
      }
      this.id = this.name + '~' + helpers.randomString();
      this.binding = options.binding || {};
      this.binding.proto = options.proto || 'tcp';
      this.binding.port = options.port || helpers.randomPort();
      this.consul_agent = new ConsulAgent;
      this.service_binding = new Binding(this.binding);
      this.service_binding.on('method', this.handleMethod.bind(this));
      this.service_binding.on('subscribe', this.handleSubscribe.bind(this));
      this.register();
      process.on('SIGINT', (function(_this) {
        return function() {
          return _this.deregister(function() {
            return process.exit();
          });
        };
      })(this));
    }

    Service.prototype.handleMethod = function(client_id, message) {
      var arity_mismatch, e, err, method_name, _method, _sendError, _sendResponse;
      if (VERBOSE) {
        log("<" + client_id + ">: " + (util.inspect(message, {
          depth: null
        })));
      }
      method_name = message.method;
      if (_method = this.getMethod(method_name)) {
        _sendResponse = (function(_this) {
          return function(response) {
            return _this.service_binding.send(client_id, {
              id: message.id,
              kind: 'response',
              response: response
            });
          };
        })(this);
        _sendError = (function(_this) {
          return function(error) {
            return _this.service_binding.send(client_id, {
              id: message.id,
              kind: 'error',
              error: error
            });
          };
        })(this);
        if (VERBOSE) {
          log('Executing ' + method_name);
        }
        try {
          return _method.apply(null, __slice.call(message.args).concat([(function(_this) {
            return function(err, response) {
              if (err) {
                return _sendError(err);
              } else {
                return _sendResponse(response);
              }
            };
          })(this)]));
        } catch (_error) {
          e = _error;
          err = e.toString();
          arity_mismatch = message.args.length !== _method.length - 1;
          if (arity_mismatch && e instanceof TypeError && err.slice(11) === 'undefined is not a function') {
            err = "ArityError? method `" + method_name + "` takes " + (_method.length - 1) + " arguments.";
          }
          return _sendError(err);
        }
      } else {
        return log.i('No method ' + message.method);
      }
    };

    Service.prototype.getMethod = function(method_name) {
      var method_context, _method;
      if (method_name[0] === '_') {
        method_name = method_name.slice(1);
        _method = this[method_name];
        return _method;
      }
      if ((method_context = method_name.split('.')).length > 1) {
        return descend(this.methods, method_context);
      } else {
        return this.methods[method_name];
      }
    };

    Service.prototype.subscriptions = {};

    Service.prototype.handleSubscribe = function(client_id, message) {
      var subscription_id, subscription_key, type, _base;
      type = message.type;
      subscription_id = message.id;
      subscription_key = [client_id, subscription_id].join(':');
      (_base = this.subscriptions)[type] || (_base[type] = []);
      return this.subscriptions[type].push(subscription_key);
    };

    Service.prototype.publish = function(type, event) {
      var subscriptions;
      if (subscriptions = this.subscriptions[type]) {
        return subscriptions.forEach((function(_this) {
          return function(subscription_key) {
            var client_id, subscription_id, _ref;
            _ref = subscription_key.split(':'), client_id = _ref[0], subscription_id = _ref[1];
            return _this.service_binding.send(client_id, {
              id: subscription_id,
              kind: 'event',
              event: event
            });
          };
        })(this));
      }
    };

    Service.prototype.handleStatus = function(cb) {
      return cb(null, {
        health: 'ok',
        uptime: process.uptime(),
        memory: process.memoryUsage(),
        load: os.loadavg()
      });
    };

    Service.prototype.register = function(cb) {
      return this.consul_agent.registerService({
        Name: this.name,
        Id: this.id,
        Port: this.binding.port,
        Tags: ["proto:" + this.binding.proto],
        Check: {
          Interval: 60,
          TTL: "10s"
        }
      }, (function(_this) {
        return function(err, registered) {
          _this.startChecks();
          log.s("Registered `" + _this.name + "` on :" + _this.binding.port);
          if (cb != null) {
            return cb(null, registered);
          }
        };
      })(this));
    };

    Service.prototype.startChecks = function() {
      return setInterval(((function(_this) {
        return function() {
          return _this.consul_agent.checkPass('service:' + _this.id);
        };
      })(this)), 9000);
    };

    Service.prototype.deregister = function(cb) {
      return this.consul_agent.deregisterService(this.id, (function(_this) {
        return function(err, deregistered) {
          log.e("Deregistered `" + _this.name + "` from :" + _this.binding.port);
          if (cb != null) {
            return cb(null, deregistered);
          }
        };
      })(this));
    };

    return Service;

  })(EventEmitter);

  module.exports = Service;

}).call(this);
