// Generated by CoffeeScript 1.7.1
(function() {
  var DEFAULT_BIND, DEFAULT_PORT, DEFAULT_PROTO, EventEmitter, SomataBinding, VERBOSE, log, randomString, util, zmq, _ref,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  zmq = require('zmq');

  util = require('util');

  EventEmitter = require('events').EventEmitter;

  _ref = require('./helpers'), randomString = _ref.randomString, log = _ref.log;

  VERBOSE = false;

  DEFAULT_PROTO = 'tcp';

  DEFAULT_BIND = '0.0.0.0';

  DEFAULT_PORT = 5555;

  module.exports = SomataBinding = (function(_super) {
    __extends(SomataBinding, _super);

    function SomataBinding(options) {
      if (options == null) {
        options = {};
      }
      this.id = this.id || randomString();
      this.proto = options.proto || DEFAULT_PROTO;
      this.host = DEFAULT_BIND;
      this.port = options.port || DEFAULT_PORT;
      this.address = this.proto + '://' + this.host + ':' + this.port;
      this.socket = zmq.socket('router');
      this.socket.bindSync(this.address);
      this.socket.on('message', (function(_this) {
        return function(client_id, message_json) {
          return _this.handleMessage(client_id.toString(), JSON.parse(message_json));
        };
      })(this));
      log("Socket " + this.id + " bound to " + this.address + "...");
    }

    SomataBinding.prototype.send = function(client_id, message) {
      return this.socket.send([client_id, JSON.stringify(message)]);
    };

    SomataBinding.prototype.handleMessage = function(client_id, message) {
      if (VERBOSE) {
        log("<" + client_id + ">: " + (util.inspect(message)));
      }
      return this.emit(message.kind, client_id, message);
    };

    return SomataBinding;

  })(EventEmitter);

}).call(this);
